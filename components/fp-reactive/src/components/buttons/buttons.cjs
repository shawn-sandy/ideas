const styles = {
  backgroundColor: 'var(--btn-bg, red)',
  display: 'inline-flex',
  minHeight: 'calc(60rem /16 )',
  placeItems: 'var(--btn-place, center)',
  paddingInline: 'var(--btn-px, 2rem)',
  border: 'var(--border, none)'
}

export default styles
